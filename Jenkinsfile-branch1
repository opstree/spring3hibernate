@Library('java-ci') _
node {
    functions.cloneRepo()
    functions.codeStability()
    functions.codeQuality()
    functions.codeCoverage()
    stage('publishing code coverage report') {
        cobertura autoUpdateHealth: false, autoUpdateStability: false, coberturaReportFile: '**/target/site/cobertura/coverage.xml', conditionalCoverageTargets: '70, 0, 0', failUnhealthy: false, failUnstable: false, lineCoverageTargets: '80, 0, 0', maxNumberOfBuilds: 0, methodCoverageTargets: '80, 0, 0', onlyStable: false, sourceEncoding: 'ASCII', zoomCoverageChart: false
    }
    stage('publishing code quality report') {
        scanForIssues tool: codeAnalysis(pattern: '**/target/checkstyle-results.xml')
    }
    workspaceCleanup()
}

def workspaceCleanup() {
    stage('work space cleanup') {
        cleanWs()
    }
}
